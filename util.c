#include "serverClient.h"

// void stringToNum(unsigned long number, char *buffer) {
//     char *ptr;
//     number = strtol(buffer, &ptr, 10);
//     //memcpy(number, result, sizeof(number)+1);
//     printf("%ld", number);
    
// }